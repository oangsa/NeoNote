use super::RpcClient;

pub fn set_colorscheme(_client: &RpcClient, _colorscheme: &str) {}

pub fn set_neovide_cursor_globals(
    _client: &RpcClient,
    _animation_length: f32,
    _trail_size: f32,
    _vfx_mode: &str,
) {
}
